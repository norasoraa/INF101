package inf101.pair;

/**
 * Interface that describes methods for extracting the numbers of a pair and the
 * sum of the numbers.
 */
public interface IIntegerPair extends IPair<Integer> {

    /** Returns the sum of the two numbers. */
    int sumOfPair();

}
