package inf101.pair;

/** Interface that describes methods for extracting objects of a pair. */
public interface IPair<T> {

    /** Gets the first object in the pair. */
    T getFirst();

    /** Gets the second object in the pair. */
    T getSecond();

}
